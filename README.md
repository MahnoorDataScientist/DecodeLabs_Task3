# DecodeLabs_Task3
This project uses unsupervised machine learning to perform customer segmentation on a mall customer dataset. It employs standard scaling for normalization, principal component analysis (PCA) for dimensional reduction, and K-Means clustering ($K=10$) to discover distinct user persona matrices visualized via an intelligence dashboard.
